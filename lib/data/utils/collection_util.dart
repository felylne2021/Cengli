enum CollectionEnum {
  groups,
  participants,
  expenses,
  users,
  orders,
  charges,
  registration
}

enum UserRoleEnum { user, partner }

enum GroupTypeEnum { general, p2p }

enum RegistrationStatusEnum { onproses, accepted }

enum ComethNetworkEnum { avax, polygon }
